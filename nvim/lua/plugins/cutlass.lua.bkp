return {
	{
		"gbprod/cutlass.nvim",
		config = function()
			require("cutlass").setup({
				exclude = { "ndd", "xd" },
			})
			vim.keymap.set("n", "dd", "dd", { noremap = true, silent = true })
		end,
	},
}
